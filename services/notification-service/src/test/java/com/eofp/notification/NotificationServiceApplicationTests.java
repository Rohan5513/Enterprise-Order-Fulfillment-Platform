package com.eofp.notification;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class NotificationServiceApplicationTests {

    @Test
    void contextLoads() {
        // Fails if the application context cannot start (bad config, missing bean, and so on)
    }
}
